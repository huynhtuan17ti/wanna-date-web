export interface User {
    id: string
    avatar_url: string
    header_url: string

    name: string
    age: number
    is_female: boolean

    short_introduce: string
    work: string
    interest: string
    location: string
    premium: boolean
}
